name: flutter_covid_dashboard_ui
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  bubble_tab_indicator: ^0.1.4
  fl_chart: ^0.9.3
  flutter_svg: ^0.19.3
  animated_text_kit: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
